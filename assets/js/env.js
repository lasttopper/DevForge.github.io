/* ============================================================
   ENVIRONMENT OVERRIDES
   ------------------------------------------------------------
   Local development: this file stays empty — the defaults in
   config.js are used.

   Production (GitHub Pages): the "Deploy to GitHub Pages"
   workflow (see .github/workflows/deploy.yml) rewrites this
   file at build time from the repository's Actions variables:

     BRAND_NAME, WHATSAPP_NUMBER, EMAIL,
     INSTAGRAM_URL, LINKEDIN_URL, GITHUB_URL

   Set them under: Settings → Secrets and variables → Actions
   → Variables. Never put secrets (API keys, tokens) here —
   these values are public in the shipped JavaScript.
   ============================================================ */
window.SITE_ENV = {};
